import React from 'react';
import ReactDOM from 'react-dom/client';

import '../index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
	<React.StrictMode>
		<div>
			<p>If you are seeing this you should be redirected to a page</p>
		</div>
	</React.StrictMode>
);
